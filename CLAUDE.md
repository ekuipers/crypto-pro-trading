# CryptoPro Trader
Description: Professional cryptocurrency trading & analytics platform. 
Creator: [name removed].
Year: 2026

> **Detail archive:** this file was compacted 2026-07-19. The complete pre-compaction text (full
> hard-rule tables with config keys, bug histories, dashboard tab specs, Node-port table, parity
> checklist) lives in **`memory/claude_md_archive.md`** — consult it before changing anything below.

# Workflow rules
> Master rules shared across all CryptoPro sub-projects: [CryptoPro Suite CLAUDE.md](https://github.com/[username]/crypto-pro-suite/blob/main/CLAUDE.md)

1. Keep a maximum of 2 months of journals and reports.
2. **Standing rule:** after every change (code, dashboard, config, scripts) update `CLAUDE.md`, `README.md`, `memory/memory.md`, `memory/glossary.md`, and — for dashboard changes — `docs/dashboard_layout.md`. No exceptions.
3. Set the backend cron jobs schedule: 
- Watchdog : every 4 hours
- Evaluation: once per day 23.00
- Forward analysis: once per day 04.00
4. Backend cron jobs should be user bound. Resulting reports should be stored related to the user in the database. Every user can then manage their own schedules set via the front end in Command center/Scheduled Jobs.
5. Always scan the Suite roadmap first

## Roadmap

1. **Reconcile cron cadence with workflow rule 3** (rule added 2026-07-24, not yet implemented). Rule 3 specifies: Watchdog every 4 hours, Evaluation once/day 23:00, Forward analysis once/day 04:00. Current actual schedule doesn't match: Evaluation runs once/day at 12:02 UTC (`trade.yml`'s paused cron `2 12 * * *`; Node dispatcher default hour 2 via `DEFAULT_HOUR_UTC.evaluate` in `src/cronSchedule.js`), Watchdog runs once/day (paused `watchdog.yml` cron `0 3 * * *`; dispatcher default hour 4), Forward analysis runs once/day at 04:08 UTC (`forward.yml`, close but not exactly 04:00). Blocker: the Node dispatcher (`src/cronSchedule.js`'s `isJobDue()`) only supports one hour-of-day trigger per job, gated on "hasn't run today" — it cannot express "every 4 hours" without a design change (e.g. an array of hours or a repeat-interval field per job). Needs a small scheduling redesign before Watchdog can actually run 6x/day.
2. **Multi-tenant conversion (Node engine only)** — satisfies workflow rule 4 ("cron jobs should be user-bound"). User confirmed (2026-07-24): full per-user Alpaca credentials (not a shared account), per-user strategy/risk config too (not just credentials), Node engine only (Python/GitHub Actions is being retired separately, untouched here). 6-phase plan, each independently shippable — full design in `memory/project-trader-multitenant-plan.md`:
   - **Phase 1 — DONE (2026-07-24).** Credential-injection seam: new `src/alpacaClient.js` (`createAlpacaClient({keyId,secret,baseUrl,dataUrl,symbolCap})` factory holding every hard rule, moved out of `trade.js`). `trade.js` is now a thin legacy shim (`defaultClient` bound to env vars, same destructured exports — zero call-site changes). `marketData.js`/`reconcile.js`/`scout.js` take an optional `{client}` override. Closed a latent gap in `runEvaluation.js`: `evaluateSymbol()`/`applyRotation()` had no credential-override seam at all even though `main()`'s other deps were overridable — now both receive a client-bound `deps`/options object. Zero schema/route/behavior changes; verified against the full 305-test suite (297 pass / 8 pre-existing unrelated failures, identical before/after).
   - **Phase 2 — encrypted credential storage.** New `src/secretsCrypto.js` (AES-256-GCM, Node's built-in `crypto`, key from new env var `TRADER_CREDENTIALS_ENC_KEY` read lazily, never a module-level constant). New table `trader_alpaca_credentials(uid, mode ['paper'|'live'], active, key_preview, ciphertext, enc_version, ...)`. New `src/credentialsRoutes.js`. **Mandatory security-reviewer pass before merge** (new auth/crypto surface).
   - **Phase 3 — per-user strategy/risk config.** New table `trader_strategy_config(uid, data jsonb)` mirroring `layouts`. `resolveConfigForUser(uid)` merges per-uid overrides over the compiled `config.json` defaults; `risk.js`'s functions already accept overrides as optional params, so multi-tenant call sites just pass resolved values explicitly.
   - **Phase 4 — schema migration + one-time data backfill.** Add `uid` to `trader_state`/`trader_journal`/`job_runs`/`cron_config` PKs/unique indexes (the `job_runs` lock especially — without `(job, uid)` two users' jobs would contend for the same lock). One-time manual SQL backfill attributes existing global rows to the current owner's uid.
   - **Phase 5 — cron dispatcher rewrite (highest-risk phase).** `handleDispatch` loops per (job, uid) instead of per job. `isOwner`/`TRADER_OWNER_UID` gating replaced by "any signed-in user manages their own rows" (`TRADER_OWNER_UID` recommended to be repurposed as an admin/diagnostic override, not deleted — confirm with user when this phase starts). Never falls back to the legacy env-var client for an unconfigured user (cross-account trading risk).
   - **Phase 6 — dashboard UI.** New, visually distinct "Server-Side Trading Engine" credential section in Settings (write-only, masked "connected" badge) — kept separate from the existing browser-only Alpaca fields (`localStorage`, already per-browser, untouched). JSON-textarea config editor. "☁ Scheduled Jobs" sub-tab needs zero UI changes — it already just calls `/api/cron/status` and shows a friendly 401 message.
   - Rollout safety: the existing owner becomes the *first* multi-tenant user (migrate their own rows to their own uid, connect existing `.env` keys through the new UI) before any other Suite account is invited to connect; legacy env-var path stays functional indefinitely as an instant rollback.
3. **Node cutover — COMPLETE (2026-07-25).** `trade.yml`/`watchdog.yml` deleted after the 2026-07-25 rescan confirmed a full automatic multi-cycle track record (see the dated rescan entries below for the evidence chain). GitHub Actions no longer plays any role in live trading; Node/Vercel is the sole live engine. Suite roadmap item 9 satisfied.
   - **Node cutover flip — CONFIRMED LIVE (2026-07-24, later same-day rescan, ahead of the planned 2026-07-25 02:00 UTC check window).** `GET /api/trader-state` on `crypto-pro-trading-bice.vercel.app` now returns `last_evaluation_iso: 2026-07-24T08:56:35.049Z` with positions `ETH/USD, AAVE/USD, LINK/USD` — diverged from the git-committed `data/positions_state.json` (`last_evaluation_iso: 2026-07-24T03:32:11...`, 4 positions incl. `SOL/USD`, now closed). Cross-checked against GitHub Actions run history (`actions/workflows/{trade,watchdog}.yml/runs`): no `trade.yml`/`watchdog.yml` run at or near 08:56 UTC, and no `schedule`-triggered run of either workflow after the `227c818` pause commit landed (10:44:48 UTC) — rules out the Python engine as the source, and Python has no Postgres `trader_state` write path at all, so this row can only have been written by the Node engine (`src/cronRoutes.js` → `db.putTraderState`). Source of the specific 08:56 UTC write (hourly dispatcher vs. a manual dashboard "Run now") is unconfirmed but doesn't affect the conclusion.
   - **Same rescan also found and fixed a live production bug that could have masked this:** `src/db.js`'s `CONN_VARS` didn't recognize the actual deployed `CRYPTOPROTRADER_POSTGRES_URL*` var names (Vercel's Supabase integration issues per-project-prefixed vars now, not the old `DBCRYPTOCHARTS_*` naming) — `connString()` returned `null`, which meant `getPool()` would throw on any Postgres call, both for auth (Suite CLAUDE.md Bug #1) and for `trader_state`/`trader_journal`/`job_runs`/`cron_config` since `cronRoutes.js` shares the same `db.js` pool. Fixed in `9cae1c7` (pushed, confirmed live via a `GET /api/session` probe returning `200` with real row data, not a `500`). The 08:56 UTC evaluation write itself predates this fix (var rename + fix both appear to have landed within today), so it isn't the reason the write succeeded — but it does mean **don't fully trust yesterday's "dispatcher had nothing due" reasoning as the only explanation for a quiet window**; a DB-connectivity failure could equally have caused a silent dispatcher error before today's fix.
   - **Not yet "stable"** — this is one confirmed divergence, not a multi-cycle track record. Keep watching `/api/trader-state` across a few more dispatcher windows before treating the engine as production-stable.
   - Per the standing instruction not to take hard-to-reverse actions without explicit go-ahead: **do not delete `trade.yml`/`watchdog.yml` yet** even though cutover is now confirmed live — ask the user first once a stable multi-cycle track record is observed (`forward.yml` stays regardless — walk-forward backtesting only, no live orders, not part of this concern).
   - **Rescan 2026-07-24 14:16 UTC — no regression, still one data point.** Re-checked GitHub Actions run history: `trade.yml`/`watchdog.yml` have had **zero** `schedule`-triggered runs since the `928e365` pause landed (09:29:46 UTC), confirmed clean across ~4h47m and multiple hourly dispatcher ticks. `GET /api/trader-state` is unchanged from the prior rescan (`last_evaluation_iso` still `08:56:35.049Z`) — expected, not a stall: `evaluate`'s `hour_utc=2` already fired once today, so `isJobDue()` correctly skips it on every later hourly tick until the UTC date rolls over. `data/shadow_run_log.jsonl`'s newest entry is `10:28:47 UTC` (still `mismatch_count: 0`, 6 clean cycles total) — its next scheduled fire is ~18:28 UTC. **Real multi-cycle confirmation still requires the 2026-07-25 ~02:00–06:00 UTC window** (when `evaluate`/`watchdog`/`daily-summary` next become due) — check `/api/trader-state` again after that. Full test suite re-run: same 8 pre-existing env-dependent failures (no local Alpaca creds in this sandbox — `apiClient.js` `buildUrl` gets `undefined` base URL), zero new failures. Multi-tenant Phase 2 (`src/secretsCrypto.js`, `src/credentialsRoutes.js`) confirmed still not started. Roadmap item 1 (cron cadence) confirmed still unimplemented — `src/cronSchedule.js`'s `isJobDue()` is still single-hour-per-job only.
   - **Rescan 2026-07-24 15:05 UTC — no regression, still one data point, ~11h from the confirmation window.** GitHub Actions run history re-checked directly via the API: latest `schedule`-triggered `trade.yml` run is `2026-07-24T03:31:52Z` and latest `watchdog.yml` schedule run is `2026-07-24T05:42:22Z` — both predate the 09:29:46 UTC pause, so still **zero** schedule runs on either paused workflow, now confirmed clean across ~5h35m. `GET /api/trader-state` unchanged (`last_evaluation_iso` still `08:56:35.049Z`, same 3 positions ETH/AAVE/LINK) — expected, same single-fire-per-day reasoning as the prior rescan. `data/shadow_run_log.jsonl` still 9 entries, newest still `10:28:47 UTC` (`mismatch_count: 0`) — its next fire (~18:28 UTC) hasn't happened yet, nothing to check. Full test suite re-run: 368 pass, 11 failing (all the same env-dependent `apiClient.js` `buildUrl` → `undefined/v2/orders` root cause as before — no local Alpaca creds in this sandbox; count differs slightly from the previously-cited "8" but every failure traces to the identical missing-credentials cause, not a new regression). Multi-tenant Phase 2 and roadmap item 1 (cron cadence) both confirmed still untouched. **Nothing actionable until the 2026-07-25 ~02:00–06:00 UTC window** — next rescan should check `/api/trader-state` for a second divergent write plus the shadow log's ~18:28 UTC entry.
   - **Rescan 2026-07-24 16:14 UTC — no regression, still one data point, ~10-14h from the confirmation window.** GitHub Actions run history re-checked directly via the API: latest `schedule`-triggered `trade.yml` run still `2026-07-24T03:31:52Z`, latest `watchdog.yml` schedule run still `2026-07-24T05:42:22Z` — both still predate the 09:29:46 UTC pause, zero schedule runs on either paused workflow, now confirmed clean across ~6h44m. `GET /api/trader-state` unchanged (`last_evaluation_iso` still `08:56:35.049Z`, same 3 positions ETH/AAVE/LINK) — expected, same single-fire-per-day reasoning. `data/shadow_run_log.jsonl` still 9 entries, newest still `10:28:47 UTC` (`mismatch_count: 0`) — its next fire (~18:28 UTC) still hasn't happened yet. Full test suite re-run: 368 pass, 8 distinct failing tests (all the same env-dependent `apiClient.js` `buildUrl` → `undefined/...` root cause — no local Alpaca creds in this sandbox — matching the originally-cited "8", no new regression). Multi-tenant Phase 2 (`src/secretsCrypto.js`, `src/credentialsRoutes.js`) and roadmap item 1 (cron cadence) both confirmed still untouched — per Suite workflow rule 26, Phase 2 is scoped to its own dedicated session, not folded into a roadmap rescan. Suite `CLAUDE.md` roadmap item 9 updated to record the redeploy as confirmed-live (previously read "not verifiable from this sandbox" — stale once the same-day evidence above landed). **Still nothing actionable until the 2026-07-25 ~02:00–06:00 UTC window.**
   - **Rescan 2026-07-24 17:00 UTC — manual `workflow_dispatch` runs found on both paused workflows; a real state-sync gap discovered, not caused.** User reported `trade.yml`/`watchdog.yml` "have run on GitHub." Confirmed: both fired via **`workflow_dispatch`** (actor `ekuipers`, 16:28–16:29 UTC, on commit `f5e5dde`) — **not** the paused `schedule:` trigger, which is still commented out and still shows zero fires since the 09:29:46 UTC pause. This is the intentional manual-rollback path the cutover plan left active on purpose (`workflow_dispatch still works for a manual run if ever needed`). Job logs confirm `trade.yml`'s run ended **"### No orders submitted"**, and `watchdog.yml`'s commit step didn't fire (no stop triggered) — the manual run placed **zero** orders and caused **no** order conflict with the Node engine.
     - **What it revealed:** Python's live reconciliation against the real Alpaca broker found **zero open positions** (`positions: {}`, committed in `5b9ca6c`, pulled into this checkout) — down from the ETH/AAVE/LINK the git-committed file still listed from before the cutover. But `GET /api/trader-state` (Postgres, what the Node engine/dashboard/Autopilot read) **still shows ETH/AAVE/LINK open** as of its last write (`08:56:35Z`), unchanged — because Node's `evaluate` already fired once today and won't run again until tomorrow. **Postgres `trader_state` is now stale relative to the real broker state.**
     - **Separately concerning:** all 3 of those Node-tracked positions have `stop_order_id: null` — Node's evaluate cycle at 08:56 never placed (or never recorded) a protective stop for them. These are pre-existing positions carried over from before the cutover (entries dated 2026-07-22), so it's unclear whether Node's stop-placement logic ran for carried-over positions on its first live cycle or skipped them. The positions are flat on the broker now, so the immediate risk has passed, but *why* they closed is unconfirmed from this sandbox (no Alpaca credentials here to pull order/fill history) — candidates: a real stop-loss fill on the exchange (hard to square with `stop_order_id: null`, unless a stop was placed and filled without ever being persisted), the browser-side Autopilot (an independent third engine, by design), or a manual close.
     - **Not yet resolved:** Postgres `trader_state` needs a resync (the next Node `evaluate` fire, or an owner-triggered dashboard "Run now") before it reflects the real flat position — until then the dashboard/Autopilot cross-engine merge is working off stale phantom positions. Flagged to the user; no further action taken from this sandbox (no `CRON_SECRET`/dashboard session available here to trigger a resync directly, and this touches live trading state).
   - **Rescan 2026-07-24 ~17:55 UTC — stale-state gap resolved; still no schedule-trigger regression, ~8h23m clean.** GitHub Actions run history re-checked via the raw API (no `gh` CLI in this sandbox): latest `schedule`-triggered `trade.yml` run still `2026-07-24T03:31:52Z`, latest `watchdog.yml` schedule run still `2026-07-24T05:42:22Z` — both still predate the 09:29:46 UTC pause, zero schedule runs on either paused workflow, now confirmed clean across ~8h23m (longer than the 17:00 UTC rescan's ~7h25m). `node-shadow-run.yml` fired again at `17:52:11Z` (`mismatch_count: 0`, 7 clean cycles, `data/shadow_run_log.jsonl` now 10 entries) — pulled 1 commit (`adb77f1`, bot-authored log append) that this checkout was behind on. **Resolved the `.7`–`.9` stale-`trader_state` finding above:** queried `job_runs`/`cron_config` directly via Postgres (credentials in local `.env`) and confirmed the owner ran a clean manual "Run now" pass at ~17:50 UTC — `watchdog` (17:50:15) → `daily-summary` (17:50:26) → `evaluate` (17:50:35), fully sequential with no overlap (the `ec75c11` mutual-disable fix held), all three `status='ok'`. `GET /api/trader-state` now returns `positions: {}`, `last_evaluation_iso: 17:50:49.150Z`, matching Python's git-committed `positions_state.json` (`16:28:53Z`, also flat) — the phantom ETH/AAVE/LINK / `stop_order_id: null` snapshot is gone, both engines agree on flat. Full test suite re-run: **305/305 passing, 0 failures** — previously-cited env-dependent failures (missing local Alpaca creds) are gone, meaning this sandbox's `.env` now has working Alpaca credentials (consistent with the Production env-var fix from the "all 3 cron jobs failed" bug above, if the same values were copied locally — not otherwise confirmed). Multi-tenant Phase 2 (`src/secretsCrypto.js`, `src/credentialsRoutes.js`) confirmed still not started. Suite `CLAUDE.md` has an uncommitted hand-drafted roadmap edit (renumbers items, adds a new "glossary in DB" item 1, drops item 9's now-past "(execute after 24-7-2026)" gate note) — not this project's file to commit, noted per `project-suite-claude-md-uncommitted-drafts` memory. **Still nothing structurally new until the 2026-07-25 ~02:00–06:00 UTC window** (next scheduled `evaluate`/`watchdog`/`daily-summary` due dates) — that window is what actually tests the automatic hourly dispatcher end-to-end again, not another manual trigger.
   - **Suite roadmap rescan 2026-07-25 19:05 UTC — multi-cycle confirmation window PASSED, awaiting user go-ahead to delete the paused workflows.** The 2026-07-25 ~02:00–06:00 UTC window the prior rescans were all waiting on has now happened and been checked. Queried `job_runs` directly (Postgres): the automatic Vercel dispatcher fired a full clean cycle overnight — `evaluate` (`triggered_by:'cron'`, started 01:01:32 UTC, `status='ok'`), `watchdog` (`cron`, 04:01:00 UTC, `ok`), `daily-summary` (`cron`, 06:03:07 UTC, `ok`) — none manual, all automatic. `GET /api/trader-state` matches (`last_evaluation_iso: 2026-07-25T01:01:34.557Z`, day rolled to 07-25, positions still flat `{}`). GitHub Actions run history re-checked directly via the API: latest `schedule`-triggered `trade.yml` run is still `2026-07-24T03:31:52Z` and latest `watchdog.yml` schedule run still `2026-07-24T05:42:22Z` — both predate the 09:29:46 UTC pause, zero schedule runs on either paused workflow, now confirmed clean across **~33h35m**, spanning this full automatic cycle plus the two prior rescan windows. `data/shadow_run_log.jsonl` now 12 entries (newest 2026-07-25 10:00:13 UTC), still `mismatch_count: 0` throughout. Full test suite re-run: **310/310 passing, 0 failures**. Multi-tenant Phase 2 and the remaining multi-language phases (Charts/Training/Mobile ports) confirmed still untouched — correctly out of scope for a monitoring rescan per Suite workflow rule 26. **This is the "stable multi-cycle track record" the standing instruction gated the `trade.yml`/`watchdog.yml` deletion on — asked the user for explicit go-ahead rather than deleting unilaterally** (hard-to-reverse action on the repo's CI config, even though `workflow_dispatch` rollback would still work either way).
   - **Suite roadmap rescan 2026-07-24 19:46 UTC — no regression, ~10h16m clean, still nothing due until tomorrow's window.** Triggered by "rescan suite roadmap" against the current `crypto-pro-suite` `CLAUDE.md` (roadmap items 0/1/9 — items under "TO DO" and "Roadmap Lemon Queezy" are explicitly planning-only, skipped). GitHub Actions run history re-checked directly via the API: latest `schedule`-triggered `trade.yml` run still `2026-07-24T03:31:52Z`, latest `watchdog.yml` schedule run still `2026-07-24T05:42:22Z` — both still predate the 09:29:46 UTC pause, zero schedule runs on either paused workflow, now confirmed clean across ~10h16m. `GET /api/trader-state` unchanged since the 17:50 UTC resync (`last_evaluation_iso: 17:50:49.150Z`, `positions: {}`) — expected, `evaluate` already fired once today. `data/shadow_run_log.jsonl` unchanged at 10 entries (newest still `17:52:11Z`, `mismatch_count: 0`) — next fire not yet due. Full test suite re-run: **310/310 passing, 0 failures** (up from 305 — the 5 new glossary-extraction tests from the same-day DB-glossary work are now part of the baseline). Confirmed still not started: multi-tenant Phase 2 (no `src/secretsCrypto.js`/`src/credentialsRoutes.js`) and Suite roadmap item 0 (multi-language — no i18n/locale scaffolding in `src/`). Per `project-suite-claude-md-uncommitted-drafts` memory and workflow rule 26 (new roadmap items get their own session), neither Phase 2 nor the multi-language item was started in this pass — both are scoped, cross-project undertakings that warrant a dedicated session rather than folding into a monitoring rescan. **Nothing actionable until the 2026-07-25 ~02:00–06:00 UTC window.**

4. **Suite roadmap item 1 — glossary moved to the database, implemented 2026-07-24.** "Add glossary to the database instead of loading it from a file." Only Trader had a file-backed glossary in this suite (Charts has none; Training's is a separate, independent hardcoded JS array, not file-loaded — out of scope for this item). New Postgres `glossary` table (`src/db.js`, single-row shape identical to `trader_state`) + `GET /api/glossary` (`src/glossaryRoutes.js`); `memory/glossary.md` stays the git-tracked, human/AI-edited source (workflow rule 2 still applies to it unchanged) and `server.js` upserts its content into the DB row on every boot. `src/js/tabs-glossary.js`'s `loadGlossary()` now calls `/api/glossary` via `fetchLocalJson` instead of fetching the file directly; the small built-in `GLOSSARY_FALLBACK_MD` constant still covers total failure (DB unset and file unreadable). **Also fixed a real latent production bug found while investigating:** `server.js` never statically served `memory/` at all (only `/js`, `/css`, `client/dist`, `docs`), so the deployed dashboard's Glossary tab could never actually reach the live file over HTTP — it had been silently stuck on the ~14-term hardcoded fallback in production the whole time, not just under `file://` as the 2026-07-18 bugfix assumed. The DB row is reachable in production, so this also closes that gap. Verified: `npm test` 305/305 passing (no regressions), a local `node server.js` boot with the real `.env` Postgres credentials showing `[db] connected; tables ready` with no sync errors, and `curl /api/glossary` returning the live ~700-line content (not the fallback). **Not verified — no browser tool this session:** an actual click-through of the Glossary sub-tab in a running browser.
   - **Scope corrected same session (user):** "I meant only the Acronyms and Trading terms from the glossary." The initial implementation synced the entire ~700-line `memory/glossary.md` (including ~30 dated implementation-changelog sections) into the DB — too broad. New `src/glossaryExtract.js` (`extractGlossarySections()`, 5 unit tests) pulls only the "Acronyms & Abbreviations" and "Trading Terms" `## `-level sections out of the file; `server.js`'s startup sync and `glossaryRoutes.js`'s file-read fallback both now run content through it before storing/serving. `memory/glossary.md` itself is untouched — still the full git-tracked source, workflow rule 2 still applies to it in full; only what gets synced into the DB/served over `/api/glossary` narrowed. Re-verified: `npm test` 310/310 passing, a live local boot + `curl /api/glossary` confirmed the DB row now holds exactly the two sections (22,709 chars, no dated changelog text).

## Bugs

- **Fixed 2026-07-24 (user confirmed, redeployed): all 3 cron jobs failed in production ~17:22–17:25 UTC — Alpaca env vars were indeed missing/broken on Production, restored by the user in Vercel.** Re-verified via `job_runs`: manual runs at 17:33 UTC all show `status='ok'`.

- **Found 2026-07-24, same verification pass: a real read-modify-write race on `trader_state` surfaced by the 17:33 UTC runs above.** Despite `evaluate` and `watchdog` both reporting `'ok'`, `GET /api/trader-state` (and the raw Postgres row, `updated_at` confirmed as freshly written at `17:33:38.546Z`) still held the *old* 08:56 UTC snapshot — same stale `last_evaluation_iso`, same 3 phantom positions Python's broker reconciliation had already shown as closed (see the `.7` rescan entry above). Root cause: `db.putTraderState()` is a blind full-blob overwrite (`data = excluded.data`, no row/optimistic locking — `src/db.js`'s `putTraderState`), and `evaluate`/`watchdog` each independently do their own load→mutate→save cycle over that same single JSON blob. The two manual "Run now" clicks overlapped — `watchdog` started (17:33:38.339) *before* `evaluate` (started 17:33:19.226, ~19s Alpaca round-trip) had finished and written back (17:33:38.649) — so whichever finished last determined the final DB content, and the mutation from the other run was silently lost. The automatic hourly dispatcher (`handleDispatch` in `cronRoutes.js`) is *not* affected — it `await`s each job strictly in sequence — this only bites concurrent **manual** triggers, which the dashboard didn't prevent. **Fix (`src/js/tabs-command.js`):** `CRON_JOBS` now tags `evaluate`/`watchdog` as `touchesState: true`; `renderCronJobs()` disables both jobs' "Run now" buttons whenever either is mid-run, and `cronRunNow()` also disables them optimistically the instant a state-touching job is clicked (closing the gap before the next status poll). `daily-summary` is untagged and stays independently clickable — it never writes `trader_state` (journal-only, per its own docstring). No backend change — considered a Postgres advisory lock, but the pooler connection string (`pooler.supabase.com:6543`, PgBouncer transaction-mode) doesn't reliably support session-scoped advisory locks across separate queries, and a `SELECT ... FOR UPDATE` transaction held open across evaluate's ~20s external API calls was judged worse (ties up one of only 5 pooled connections for the duration). **Follow-up completed 2026-07-24 ~17:50 UTC:** a clean manual "Run now" pass (watchdog 17:50:15 → daily-summary 17:50:26 → evaluate 17:50:35, sequential, no overlap — confirming the mutual-disable fix holds) all returned `status='ok'` and corrected the stale row: `GET /api/trader-state` now shows `positions: {}`, `last_evaluation_iso: 2026-07-24T17:50:49.150Z`, matching Python's git-committed `positions_state.json` (also flat, `16:28:53Z`). The stale-snapshot / phantom-position concern from the `.7`–`.9` rescan entries is resolved.

- **Open 2026-07-24: all 3 cron jobs (evaluate/watchdog/daily-summary) fail in production since ~17:22 UTC — likely Alpaca env vars missing/broken in Vercel.** Queried `job_runs` directly (Postgres): the 08:56 UTC cron-triggered runs of all 3 jobs succeeded (`status='ok'`); every run since 17:22 UTC (both `manual` "Run now" clicks and the hourly `cron` dispatch) fails (`status='error'`, generic `"<job> failed (see logs)"` detail). No code touching `alpacaClient.js`/`trade.js`/`cronRoutes.js`/`env.js` was deployed in that window (git history confirms the last change to those files predates the 08:56 success), which rules out a code regression — this points at the Vercel *environment*, most likely changed by the redeploy that added `TRADER_OWNER_UID` (see the fixed bug below, same redeploy). The exact failure the user saw in Vercel's logs — `FAIL: daily summary: TypeError: Invalid URL` — is `dailySummary.js`'s own `console.error("FAIL: daily summary: " + e)`, thrown from `getAccount()`/`getPositions()` (bound to `trade.js`'s `defaultClient`, which reads `APCA_API_KEY_ID`/`APCA_API_SECRET_KEY`/`APCA_BASE_URL` from `process.env`) — `new URL(undefined + "/v2/account")` throws exactly this when `APCA_BASE_URL` is unset. Evaluate/watchdog fail the same way (same underlying client). **Action needed (Vercel dashboard access required, not available from this sandbox): confirm `APCA_API_KEY_ID`/`APCA_API_SECRET_KEY`/`APCA_BASE_URL` are set on the *Production* environment (not just Preview/Development) and redeploy if fixed.** Until this is fixed, Postgres `trader_state` cannot self-correct — it's been stuck at the 08:56 snapshot (3 phantom positions) since Node can't complete a cycle. **Caution:** `CRON_EXECUTE=true` is live in production, so once Alpaca connectivity is restored, the next successful `evaluate`/`watchdog` run will place real (paper) orders if conditions are met — don't blind-trigger `/api/cron/evaluate|watchdog` for diagnostics once this is fixed (`/api/cron/daily-summary` is safe to re-test — it's read-only, places no orders).

- **Fixed 2026-07-24: Scheduled Jobs panel showed no schedules and no "Run now" button for anyone, including the owner.** Root cause: `TRADER_OWNER_UID` was never set in the Vercel production env (only documented as a commented-out example in `.env.example`), so `src/cronRoutes.js`'s `isOwner()` unconditionally returns `false` (`if (!OWNER_UID) return false;`) — even the real owner signing in falls into `GET /api/cron/status`'s 401 branch, and `src/js/tabs-command.js`'s `renderCronJobs()` renders only "Sign in as the configured owner account..." with no table and no buttons. Confirmed the correct value by querying the shared `accounts` table directly (Postgres, via the credentials now in `.env`): the only real owner-shaped account is `id='ekuipers'`. Set `TRADER_OWNER_UID=ekuipers` in the local `.env` for dev/testing. **Still open: this must also be set in Vercel's dashboard env vars and redeployed** (same "env var change needs a redeploy to take effect" caveat as `CRON_EXECUTE`) — not done from this sandbox, no Vercel deploy access here.

## Hosting & frontend
- Live trading engine = Node.js via Vercel Cron (`/api/cron/dispatch`), since the 2026-07-25 cutover completion (see "Node.js port" below) — `trade.yml`/`watchdog.yml` deleted, GitHub Actions no longer involved in live trading. `forward.yml` (walk-forward backtesting, no live orders) and `node-shadow-run.yml` (parity monitoring, now vestigial post-cutover) remain.
- Dashboard = React/Vite shell (`client/`) + 30 unchanged classic-global `src/js/*.js` files + 10 `src/css/*.css`. `server.js` serves `client/dist` (`npm run build` first, then `npm start`/`npm run dev`). No `file://` or GitHub Pages; Vercel works.
- `client/` is its own npm project, not an npm workspace — root `npm run build` runs `npm --prefix client install && npm --prefix client run build` (not just `... run build`) so a fresh clone (Vercel) installs `client/`'s own `vite`/`react` deps before building. Fixed 2026-07-19 after Vercel failed with "vite: command not found"; details `memory/memory.md`.
- React owns only the shell chrome; vanilla `switchTab()` owns all tab switching/loaders. Browser click-through **not yet verified** — exercise Autopilot toggle, Settings save, and every tab before relying on it. Details: archive › Dashboard.
- **Auth / SSO (2026-07-19):** `src/auth.js` + `src/db.js` + `src/totp.js` (ported from CryptoPro Charts/Suite) add a `👤 Sign in` header button — username/password + optional TOTP 2FA, accounts/sessions in Postgres. `db.js`'s `CONN_VARS` prefers `DBCRYPTOCHARTS_POSTGRES_URL[_NON_POOLING]` (the suite-shared DB) over this project's own pre-existing `trading_POSTGRES_URL*` vars, so accounts are shared suite-wide — **but only once the deployed env actually points at Charts' Supabase project**; that's a Vercel dashboard step, not done yet (see `.env.example`). Client: `src/js/auth.js` (classic script, added to `client/src/scriptLoader.js`'s `SCRIPT_ORDER`) + `#authModalBackdrop` in `client/src/fragments/modals.html`. **2FA QR code (2026-07-22, Suite roadmap):** the "Enable 2FA" modal now renders the `otpauthUri` `/api/auth/2fa/setup` already returned as a scannable QR image (`src/js/qrcode-lib.js`, vendored `qrcode-generator` — MIT, pure JS, no CDN — loaded just ahead of `auth.js` in `SCRIPT_ORDER`), not just the raw secret text. Same change landed identically in Suite/Charts/Training — details `memory/memory.md` v2026-07-22.6. **Notification email (2026-07-22, Suite roadmap):** the account modal gained a "Notification email" field (`accounts.notification_email`, new `POST /api/auth/notification-email` route) — an address the account can be reached at, unrelated to sign-in. Captures and persists only; no notification is actually sent anywhere yet (no SMTP provider configured). Same change landed identically in Suite/Charts/Training — details `memory/memory.md` v2026-07-22.7.
- **User manual (2026-07-22, Suite roadmap item 1):** `📖 Help` button (`❓`, `client/src/components/Header.jsx`, next to the theme toggle) opens `#manualPanel` — an off-canvas panel that unfolds from the left (`src/css/manual.css`, `.manual-overlay.open`), consistent with the panel Charts already ships. Content is a static `MANUAL_SECTIONS` array in `src/js/manual.js` (classic global script, added to `scriptLoader.js`'s `SCRIPT_ORDER`) covering every tab group plus Account/sign-in and keyboard shortcuts, with a left-rail TOC and a live search box filtering by title/body. No markdown fetch, no network call — content is hand-written and ships with the bundle.
- **Multi-language support — EN/NL/FR/ES, Phase 0 (2026-07-24, Suite roadmap item 0; full-suite plan in `memory/i18n-suite-plan.md`):** `client/src/i18n/` adds `i18next` + `react-i18next`, initialized in `main.jsx` before the app renders. The ~30 classic-global `src/js/*.js` dashboard scripts and the raw-HTML tab/modal fragments (`dangerouslySetInnerHTML`) can't use React hooks, so `i18n/index.js` also exposes a plain `window.t()` and a generic `applyDomI18n(root)` that walks `data-i18n`/`data-i18n-html`/`data-i18n-placeholder`/`data-i18n-title` attributes — one mechanism covers both the JSX shell and the static markup. A language switcher (🇬🇧/NL/FR/ES `<select>`, styled as `.theme-btn`) sits in the header next to the theme toggle; the choice persists to `localStorage`'s `dashLang` key, which `settings-sync.js`'s `SETTINGS_SYNC_KEYS` already round-trips through `/api/session` (same server-wins pattern as theme/last-tab — no new sync code needed). **Shipped this pass:** Header/Footer/Nav (JSX, `useTranslation()`) and the trade/journal/manual/terms modals (`modals.html`, `data-i18n` attributes) — the `common` namespace (`client/src/i18n/locales/common/{en,nl,fr,es}.json`), meant to be ported identically to Charts/Suite/Mobile per this codebase's established shared-chrome convention. Verified with a headless-Chromium pass (Playwright): switching to NL correctly re-renders nav labels, footer text, and the terms modal (HTML bold tags intact), no console errors.

**Phase 0b shipped (2026-07-25) — the 13 tab HTML fragments + auth.js/manual.js chrome:** new `app` i18next namespace (`client/src/i18n/locales/app/{en,nl,fr,es}.json`, 550 keys each, verified identical key sets across all 4 languages) covers `client/src/tabs/*.html`'s subnav buttons, section/panel/chart titles, period and filter buttons, table column headers + tooltips, and loading/empty-state placeholders across all 13 tabs (command, analytics, risk, execution, signals, backtest, market, markov, insights, scalp, port-overview, port-dist, settings). `i18n/index.js` gained a `data-i18n-tip` attribute handler (mirrors `data-i18n-title`, sets `el.dataset.tip` — the custom tooltip system in `ui-helpers.js` reads that attribute live on hover, not at parse time, so translating it works across a language switch) alongside `ns: ['common', 'app']`. `src/js/auth.js`'s sign-in/register/change-password/2FA-setup/2FA-disable/account modals all now build their markup via `window.t('app:auth.*')` calls at render time. `src/js/manual.js`'s 8 section titles moved from a baked `title: "..."` string to a `titleKey` + `manualTitle()` helper that calls `window.t()` at render time (not module-load time), so the TOC and header re-translate correctly if the manual is opened after a language switch — a plain baked string would have frozen at whatever language was active when the module first evaluated.

**Explicitly deferred, not overlooked:** (1) long free-form explanatory paragraphs — command.html's Autopilot description, the News/Socials/Glossary explainer paragraphs — stay English-only; dense multi-sentence prose where translation quality matters more, lower priority than the mechanical UI-label pass per the task scope. (2) Two spots with a live DOM span the underlying vanilla JS updates via `textContent` after load — markov.html's `#mkThreshLabel` (threshold %, `tabs-markov.js`) and port-overview.html's `#portPosCount` (position count, `tabs-portfolio.js`) — were left as English templates; wrapping either in `data-i18n-html` would work on first render but a later language switch re-running `applyDomI18n` would reset the span to the template's static placeholder, clobbering the live value. (3) `manual.js`'s actual section body HTML (the help documentation prose) stays English — only the 8 section titles/TOC entries were translated, per the task's explicit priority ordering. Verified: `cd client && npm run build` (84 modules, no errors, up from 80 with the 4 new JSON files) and `npm test` (310/310 passing, unchanged) both pass; a Playwright headless-Chromium pass against the Vite dev server confirmed NL renders correctly across Command/Analytics/Signals/Settings tabs (subnav, section titles, table headers, and the new tooltip mechanism via `data-i18n-tip` all switch language correctly) with no new console errors (the one console message seen — "Add your Alpaca API key and secret in Settings first." — is a pre-existing app guard for missing credentials in the test browser profile, unrelated to i18n). Full remaining scope (Charts/Suite/Mobile port, Training chrome + full 67-module content translation) tracked in `memory/i18n-suite-plan.md`.
- **Cross-project SSO ticket (2026-07-20):** `db.createSsoTicket`/`consumeSsoTicket` + a `POST /api/auth/sso-ticket` route and an `?sso=<token>` consume middleware in `src/auth.js` (single-use, 60s TTL) let this app accept an auto-sign-in handoff from a sibling CryptoPro app — same shared `sso_tickets` table, ported identically to Charts/Training/Suite. Suite's landing page is the only issuer today; this app only consumes. Details: `memory/memory.md` v2026-07-20.1, full cross-repo narrative in `CryptoPro Suite/memory/memory.md`.

## Node.js port (cutover complete 2026-07-25 — Node is now the live engine)
- `src/*.js` ports order execution + the evaluation loop incl. scout, plus (2026-07-21) `stopWatchdog.js` and `dailySummary.js` — every script the old GitHub Actions workflows called now has a Node equivalent (310 tests, `npm test`; `npm run evaluate` dry-run).
- Cutover gated on 4 checks — status as of 2026-07-24 (roadmap rescan):
  1. **Decision parity — evidence gathered, not a true frozen-fixture replay.** Python doesn't support injected fixtures (no dependency-injection design like Node's `deps`), so `scripts/verify_decision_parity.py`/`.mjs` instead call `evaluate_symbol()`/`evaluateSymbol()` directly (bypassing state/journal writes entirely — read-only) against the SAME live positions/account/market data, run concurrently. Two concurrent trials: **10/10 watchlist symbols matched exactly** on action + every indicator field. A first *sequential* trial showed BTC/ETH mismatches, traced to a one-bar timing skew between the two process calls on the two most actively-traded pairs (not a logic bug) — confirmed by the concurrent re-runs matching exactly. See `memory/memory.md` v2026-07-21.8.
  2. **≥24h live shadow-run parity — PASSED (2026-07-24 rescan).** New `.github/workflows/node-shadow-run.yml` runs independently of `trade.yml` (own schedule, own concurrency group, never touches `positions_state.json`/`journal/*.md`/order placement) every 8h, diffs Python vs Node decisions, and appends to `data/shadow_run_log.jsonl` (`scripts/shadow_run_diff.py`). **2026-07-22 rescan:** the workflow missed its first 3 scheduled windows with zero log entries; root cause was missing/unscoped Alpaca credentials (fixed). First genuine automated entry landed 2026-07-22 19:31 UTC. **2026-07-24 rescan:** the repo went public, letting the GitHub Actions run history be checked directly (`api.github.com/repos/.../actions/workflows/.../runs`) — the earlier "possible missed window" finding from the same day's first rescan pass was a false alarm caused by an out-of-date local clone (this environment's checkout was 3 commits behind `origin/main`; `git pull --rebase` fixed it). The log now holds **5 clean automated cycles** (2026-07-22 19:31, 2026-07-23 03:44/10:35/17:46, 2026-07-24 03:43 UTC), all `mismatch_count: 0`, spanning **~32h12m** between the first and most recent entry — comfortably past the ≥24h bar. Gate 2 is satisfied. Details: `memory/memory.md` v2026-07-22.4/.5, v2026-07-24.1/.2.
  3. **State-file round-trip Python↔Node — PASSED.** `scripts/verify_state_roundtrip.py` round-trips the real committed state plus a synthetic fully-populated position through the actual `load_state()`/`save_state()` (Python) and `loadState()`/`saveState()` (Node) — both directions lossless.
  4. **Vercel function execution-time budget — PASSED (live production test, 2026-07-24 rescan).** Local dry-run timing had suggested `runEvaluation.main` ~37.7s (`debug/time_cron_jobs.mjs`); direct GET requests (with the `CRON_SECRET` bearer) against the deployed `/api/cron/evaluate`, `/api/cron/watchdog`, `/api/cron/daily-summary` routes on `crypto-pro-trading-bice.vercel.app` returned `200 {"ok":true,"code":0}` in **5.7s, 1.4s, and 1.1s** respectively — far under the `maxDuration: 120` budget, even summed. Production is faster than the local dry-run proxy suggested.
  - **All 4 gates PASSED, and the cutover is now fully complete (2026-07-25).** `trade.yml` and `watchdog.yml` have been deleted outright — the user gave explicit go-ahead on this rescan after the 2026-07-25 ~01:00–06:00 UTC window showed a full automatic `evaluate`(01:01)→`watchdog`(04:01)→`daily-summary`(06:03) cycle, all `job_runs.triggered_by='cron'`, all `status='ok'`, on top of ~33.5h of zero `schedule`-triggered runs on the paused workflows. `forward.yml` (walk-forward backtesting, no live orders) and `node-shadow-run.yml` (parity monitoring — now comparing Node against a Python engine that no longer runs live, so its ongoing value is limited to a sanity check; consider retiring it in a future pass) were deliberately left alone.
  - **Cutover history (2026-07-24, user approved):** `trade.yml`'s and `watchdog.yml`'s `schedule:` triggers were first commented out (not deleted — instant rollback, `workflow_dispatch` still worked manually) so they'd stop placing real orders once `CRON_EXECUTE=true` went live on Vercel. **`CRON_EXECUTE=true` was saved in the Vercel dashboard, but Vercel does not apply an env var change to an already-built deployment — it only takes effect on the next deployment.** A redeploy is required after any env var change; don't assume "saved" means "live" without one.
  - **Autopilot is a third, intentionally-independent trading loop — by design, not an oversight.** The dashboard's browser-side Autopilot (`src/js/autopilot.js`) is meant to *coexist* with whichever cron engine is authoritative (Python/GitHub Actions today, the Node/Vercel engine after cutover): Autopilot only runs while a browser tab is open, so it can react faster when the user is watching, while the always-on cron engine covers the gaps (overnight, asleep, browser closed) so buy/sell signals are never missed. **Fixed 2026-07-24 (confirmed during the gate-4 close, live testing on production surfaced this as an unaccounted-for third actor):** Autopilot's cross-engine merge (HWM/partial-TP/entry-time, roadmap item 10) previously only ever fetched the static `data/positions_state.json` file — accurate for Python, but that file goes stale once Python is retired. New `GET /api/trader-state` (`src/cronRoutes.js`, unauthenticated, same trust level as the static file it supersedes) returns the Postgres `trader_state` row when present, else falls back to the file — `src/js/autopilot.js` and `src/js/tabs-command.js`'s split-HWM warning now try this endpoint first. This means Autopilot keeps merging correctly regardless of which engine is authoritative, both before and after the cutover.

## Cron cutover (Suite roadmap, "For Trader only", 2026-07-21 — NOT live yet)
- `src/cronRoutes.js` (`GET/POST /api/cron/evaluate|watchdog|daily-summary`, plus `GET /api/cron/dispatch`) runs the Node engines as Vercel Cron-triggered serverless functions instead of GitHub Actions. `GET` (the Vercel Cron contract) requires the `CRON_SECRET` bearer header; `POST` (dashboard "Run now") requires signing in as the single `TRADER_OWNER_UID` account — see `.env.example`. State/journal persist to Postgres (`trader_state`/`trader_journal` tables in `src/db.js`, not `positions_state.json`/`journal/*.md`) since a serverless function has no persistent local disk; `job_runs` is the audit trail + concurrency lock (one `status='running'` row per job, DB-enforced via a partial unique index).
- **Adjustable schedule (2026-07-21 follow-up):** a single dispatcher (`/api/cron/dispatch`) reads each job's dashboard-configured `hour_utc` from `cron_config` and only runs it once that UTC hour arrives and it hasn't already run today (`src/cronSchedule.js`'s `isJobDue()`, unit-tested). The individual `/api/cron/<job>` routes are untouched and still run immediately/unconditionally when hit directly (manual trigger or direct bearer call) — `hour_utc` only gates the dispatcher.
- **Vercel plan upgraded to Pro (2026-07-21):** the Hobby plan's once-daily cron limit briefly blocked deployment (an hourly `vercel.json` entry was rejected at deploy time) and was worked around with a GitHub Actions hourly pinger (`cron-dispatch-ping.yml`). That workaround was Suite roadmap item #1 ("remove all GitHub workflow code, Vercel executes autonomously") in reverse, so the project was upgraded to **Vercel Pro** instead: `vercel.json`'s cron now runs `/api/cron/dispatch` natively every hour (`0 * * * *`), and `cron-dispatch-ping.yml` has been deleted — no GitHub involvement in triggering the dispatcher anymore.
- **Roadmap item #1 partially done:** the dispatcher no longer needs GitHub. `trade.yml`, `watchdog.yml`, and `forward.yml` still run the **live Python engine** on GitHub Actions and are intentionally NOT removed yet — they're the production trading engine, and retiring them means cutting over to the Node port, which is still gated on the 4-check parity process below. Removing them now would stop the live paper-trading loop. Full remaining scope tracked in Suite `CLAUDE.md` roadmap.
- **`CRON_EXECUTE` defaults unset/false — these routes are dry-run only today.** GitHub Actions stays the live engine until the Node.js port's 4-gate cutover checkpoint above passes; this exists so the infrastructure (routes, DB schema, dashboard panel) is built and testable ahead of that decision, not so it goes live automatically. Full analysis: `memory/memory.md` v2026-07-21.3/.4/.5/.6/.7.
- Command tab's "☁ Scheduled Jobs" panel (owner-only, `/api/cron/status`) shows last-run status per job, a per-job enabled toggle, an hour-of-day selector (UTC) for the adjustable schedule, and a manual "Run now" trigger. `cron_config.updated_by_uid` records who last changed a job's config — one shared schedule per job, attributed to the account that set it, not a separate schedule per account (this is a single-tenant trading engine).

# Trading agent
Autonomous paper-crypto agent on Alpaca. Crypto trades 24/7 — no weekday/market-clock gate. All times GMT+2.
**Always read `skills/crypto-trader/SKILL.md` before evaluating any trade.** Other skills: `crypto-catalysts` (news severity ladder — defensive only, never justifies an entry), `hourly-research`, `morning-brief`, `daily-journal`.

## Schedule
- **Hourly on the hour:** research every watchlist symbol per `skills/hourly-research-SKILL.md` (`Research HH:MM GMT+2` block).
- **Daily 12:02 UTC:** `scripts/run_evaluation.py --execute` (CADENCE WARNING if previous run >25h old; state file committed every run). **Cost-throttled cadence history:** hourly (spec, Bug #4 2026-07-10) → every 8h (2026-07-20) → once/day (2026-07-24, `trade.yml`'s cron `2 12 * * *`), pending Vercel Pro before revisiting — the user's stated plan is every-4h once purchased. Every intentional cadence change here must also update `CADENCE_WARNING_MIN`/`CADENCE_EXPECTED_MIN` in `run_evaluation.py` **and** `runEvaluation.js` (kept in parity), or the self-monitor false-fires "stops were unchecked in the gap" on every single run — as it silently did for 4 days straight (2026-07-20 through 2026-07-24) after the 8-hourly change went in without the threshold being updated. See `memory/memory.md` v2026-07-24.1.
- **Daily 03:00 UTC:** `scripts/stop_watchdog.py --execute` via `watchdog.yml` — open-long exit levels only. Throttled from the original every-5-min cadence 2026-07-20 to cut GitHub Actions minutes (`memory/memory.md` v2026-07-21.3). Note: with evaluation now only once/day, this daily watchdog run is the only automated stop-loss check outside that single evaluation cycle — trailing-stop arming/HWM updates and partial-TP still only advance during the once-daily evaluation.
- **Daily 23:21:** closing journal (`scripts/daily_summary.py` appends the Daily Summary block).

## Hard rules — never break (full table + config keys: archive › Hard Rules)
- Preserve ≥20% cash. Per-symbol caps (`config.json › portfolio_caps.caps`, % of equity): BTC 30, ETH 15, ADA/SOL 10, DOGE 8, LTC/DOT 6, LINK/AVAX/AAVE 5, default 5.
- **Limit orders only** (≤0.2% from ask; 0.5% for stops, clamped to band edge, never self-rejected). **All orders via `scripts/trade.py`** — direct API calls forbidden. Stop dedup: check `get_open_orders`, cancel-replace wider after 2 cycles.
- Long stop = previous 4H swing low (20 bars, ≤8% below entry; fallback −5%). Short stop = +5% adverse. Trailing stop arms at +2.5% profit, trails 3% below HWM (persisted in `data/positions_state.json`). Never move a stop away from entry.
- Partial TP at +1R: sell 50%, remaining stop → breakeven (effective stop = max(swing low, breakeven)). Stale exit: >48h held, trail never armed, score <2.5. Rotation at full budget: candidate ≥4.0 and ≥2.0 above weakest holding ≤0 → swap, max 1/cycle.
- Correlation budget: **7 total / 5 per tier** (Tier-1 = BTC, ETH; Tier-2 = rest). BUDGET EXCEEDED warning when positions exceed it (budget gates entries only). Net R:R soft entry gate: <1.0 block, <1.5 half-size (target BB-upper minus round-trip cost = 2×25 bps + spread).
- Streak throttle (**ACTIVE**): 3 straight losing round-trips OR 7-day drawdown ≥5% → risk ×0.5; releases after 2 winners AND drawdown <2.5%.
- Score gates: long ≥3.5 full / ≥2.5 half (daily not downtrend); counter-trend half-long ≥4.0 in a downtrend. TA sell at ≤−2. **Shorts disabled** (Alpaca spot; cover logic stays at ≥+2 / +5% stop). Close any position flagged by research, before TA signals.
- ATR sizing: risk = equity×1%; stop dist = 1.5×ATR; qty = risk/dist; hard-capped by symbol cap. (Exit stop is the swing low, so realized risk can differ from 1%.)
- Ships-OFF flags — never enable without reading archive detail (`assertNotShipped()` guards the Node side): chandelier trail, pyramiding, conviction sizing, measured-move target, maker-first entries, breadth gate. Session-edge filter is **ON** (half-size in negative-expectancy GMT+2 buckets, ≥20-sample guard).
- Journal every day, even quiet ones ("No trades — reason: …").

## Method
- Top-down: daily regime (last vs SMA50 + SMA20 vs SMA50) → 4H structure (EMA20/50 = primary trend filter) → 15-min execution. Only trade with the 4H + daily trend. Wyckoff awareness: accumulation/mark-up = long; distribution/mark-down = take profit / stay flat.
- 6-point Signal Confluence score (auto-computed by `run_evaluation.py`): EMA cross, MACD histogram, RSI, Bollinger %b, volume ratio, 4H regime. Full table, entry/cover rules, decision checklist, sizing examples, common mistakes: archive.
- Log every evaluation to `journal/YYYY-MM-DD.md` (block format in archive › Output Format); one line per symbol on all-HOLD hours.

## Modules
- `scripts/scout.py` — promotes ≤3 uptrending high-confluence `*/USD` pairs (score ≥4) into `data/watchlist_dynamic.json` (TTL 6h, merged when `scout.enabled`). Analysis-only.
- `scripts/rebalance.py` — aligns positions to caps (over-cap SELL immediate; under-cap BUY gated by score ≥3 + regime). Manual; dry-run default, `--execute` to submit.
- `.claude/agents/market-researcher.md` — research/verification subagent; **invoke after every strategy change**; writes reports to `data/market_research/`; never trades or edits code.

## Dashboard (details: archive › Dashboard + `docs/dashboard_layout.md`)
- Left-sidebar nav (Command / Trade / Portfolio / Analysis / Settings), hash deep links + `localStorage.lastTab`. Command hosts the Autopilot loop (always OFF on page load; its orders tagged `client_order_id` `ap-`; kill switch cancels all orders) plus News/Socials/Glossary sub-tabs. Settings persist to `localStorage`, seeded from `config.json` (load-only fallback).
- **☁ Scheduled Jobs sub-tab (2026-07-21, Suite roadmap — cron cutover; adjustable schedule same day; split into its own Command sub-tab next to Overview 2026-07-22, Suite roadmap rescan):** Owner-only (`TRADER_OWNER_UID`); shows last-run status/time per job (evaluate/watchdog/daily-summary) from `/api/cron/status`, a per-job enabled toggle, an hour-of-day (UTC) schedule selector, and a "Run now" manual trigger. See "Cron cutover" above — dry-run only (`CRON_EXECUTE` unset) until the Node/Python parity checkpoint passes. **"Run now" progress indicator (2026-07-24, user-reported: clicking it gave no visible feedback):** the POST doesn't resolve until the job fully finishes (`evaluate` takes ~20s for its real Alpaca round-trip), so `cronRunNow()` now marks the job "running" in a local `_cronLocalRunning` map *before* the request goes out, and a 1s ticker (`_cronTicker`) re-renders while it's outstanding — the row shows the existing `.spinner` CSS class plus a live "running… (Ns)" elapsed-time label instead of just sitting on a disabled button. Also folded in the Evaluate/Watchdog mutual-disable guard (previous fix) so both concerns share one `thisRunning`/`stateJobRunning` computation in `renderCronJobs()`.
- **Footer disclaimer + Terms of Service (2026-07-22, Suite roadmap rescan):** the footer now carries a trading-risk disclaimer (paper by default; live trading can incur real losses, at the user's own risk) and a "Terms of Service" link opening a static in-app modal (`src/js/terms-modal.js`, `#termsModalBackdrop` in `modals.html`) — no financial advice, no warranty, user accepts risk when enabling live trading. No network call.
- **Settings sync (2026-07-21, Suite roadmap):** `src/js/settings-sync.js` mirrors theme, last tab, watchlist, backtest-form defaults, and the non-secret parts of dashboard settings (mode, position limits) to Postgres (`layouts` table, one row per account keyed by `db.SESSION_NAME`, via `/api/session` GET/PUT) so they follow the signed-in account across devices/browsers — server wins whenever it has data, same precedent as CryptoPro Charts' `persistence.js`. **Deliberately excluded, stays local-only:** Alpaca API key/secret (paper and live) and all `autopilotXxx` runtime keys (HWM, partial-TP, entry time, order age) — syncing live trading credentials or cross-device Autopilot bookkeeping was scoped out on purpose (memory/memory.md v2026-07-21.2).
- **Scoring parity:** `calcSignalScore()` (JS) must stay identical to `indicators.py signal_score()`. On any indicator change run the 15-point checklist (archive › Python ↔ Dashboard consistency check): EMA seeding + ±0.05% dead zone, MACD NaN-stripped signal line + partial credits, RSI rising rule, thresholds 3.5/2.5/4.0 (never `=== 3`), BB population std-dev, volume window excl. current bar, daily-regime SMA rule, ATR sizing, bar completeness (`end = now − 1 bar`) and recency, annualization **365**, min **60** bars, `STRAT_CFG` seeded from `config.json`, trade-economics function pairs.
- **Reconciliation parity (2026-07-20 lesson):** `reconcile_positions_from_fills()` exists in **3 places** — `scripts/run_evaluation.py` (Python live engine), `src/js/edge-insights.js`'s `apReconcileFromFills()` (browser Autopilot — places real orders independently whenever toggled on), `src/reconcile.js` (Node port). Any fix to the FIFO/flatness/dust-tolerance logic must be applied to all three — a Bug #9 fix landed in Python only first, and the browser Autopilot kept round-tripping real orders for another day before the other two copies were found. See `memory/memory.md` v2026-07-20.2.
- ADX/OBV are informational only — never fold into the score. Never revert the FILL fetch to a single page — realized P&L must use `edgeFetchAllFills()` everywhere (P&L, Backtest vs Live, journal all agree via `computeFifoStats()`).
