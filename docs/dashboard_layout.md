# Dashboard Layout

Design reference for the dashboard: philosophy, navigation, tab structure and keyboard
shortcuts. **Not a changelog** — `git log` is the history. Keep this in step with layout
and navigation changes (CLAUDE.md rule 1).

---

## Design Philosophy

A trader dashboard should not just show how much money is being made — it should answer:
**"Am I allowed to trade, and am I trading correctly?"**

Key principles applied across both dashboards:

- **Decision cockpit first** — risk status, cash reserve, and open exposure are shown before P&L.
- **Hard rules in the UI** — CLAUDE.md's hard rules are enforced in code (`src/trade.js`) and surfaced visually so they can never be forgotten.
- **Colour grammar** — green = within limits, yellow = approaching a limit, red = rule breached or action required.
- **React (Vite) shell + Node.js server** — `client/` builds the shell, `server.js` serves it: run `npm run build` once, then `npm start`. Not openable via `file://`. Tab bodies and all business logic are vanilla `src/js/*.js`; only the shell (header/nav/layout) is React.
- **Alpaca API** — all live data is fetched directly from the Alpaca paper/live API using credentials entered in Settings.
- **Engine ↔ dashboard parity** — the in-browser `calcSignalScore()` must mirror the engine's `signalScore()` exactly. Enforced by `src/scoreParity.test.js`; see CLAUDE.md.

---

## Dashboard — `client/` (React shell) + `src/js/` + `src/css/`

**Status:** Primary (recommended)
**Title:** "CryptoPro Trader"
**Tabs:** 13 top-level (three with sub-tabs), grouped under sidebar section labels — 🧭 Command [Overview · **☁ Scheduled Jobs** (per-account cron monitor — owner-only until multi-tenant Phase 5, 2026-07-28; split into its own sub-tab 2026-07-22 — was a section within Overview) · **📰 News** (aggregated headlines, added 2026-07-09) · 🐦 Socials · **📖 Glossary** (renders `memory/glossary.md`, added 2026-07-18) · **🧪 Manual Trading** (paper-trading sandbox — open/close positions by hand, scored on the 6-point scale, added 2026-08-08)] · ⚡ Trade [Signals · **⚡ Scalping** (low-TF 5m/15m/1h confluence scanner + manual Buy/Sell, added 2026-06-19) · **Market** (Overview · Scanner · Breakout sub-tabs) · Execution] · 💼 Portfolio [Overview · Allocation · Risk] · 📊 Analysis [**🔬 Analytics** (Performance · P&L · Edge sub-tabs) · 🧠 Insights · Backtest vs Live · Markov] · ⚙ Settings *(Positions dropped 2026-06-17 — its table lives in Portfolio Overview)*

### Navigation & layout

- **Left sidebar navigation** — a `.layout` flex wrapper holds `<nav>` + `<main>`; `nav` is a 210px sticky vertical column, the active tab marked by a left blue border + tint. Tabs are **grouped by job-to-be-done** under `.nav-section-label` headers (⚡ Trade / 💼 Portfolio / 📊 Analysis), with Command and Settings as ungrouped anchors top and bottom — an Act → Hold → Analyze flow. Three parent tabs nest sub-tabs via a shared system (`.subnav` / `.subpage` / `.subtab-btn`; generic `_activateSubTab(parentId, subId)` scoped to `#page-<parent>`; parent resolution via `subParentOf(id)` / `subTabFnOf(parent)`): 🧭 Command (since 2026-07-09), 🌐 Market and 🔬 Analytics.
- **Mobile (≤700px)** — `.layout` switches to a column and `nav` collapses to a horizontal scrolling bar with a bottom-border active marker. All tables scroll horizontally (`overflow-x` on `.table-wrap`, clamped to `calc(100vw - 32px)`), so the page is fully usable in portrait.
- **Tab deep-linking + refresh memory** — the active tab is reflected in the URL hash (e.g. `#signals`). `switchTab()` writes the tab id to the hash (`history.replaceState`) and to `localStorage.lastTab`; on load `applyTabFromUrl()` (end of `bootstrapDashboard()`) restores the tab from the hash first, then `localStorage`. A `hashchange` listener switches tabs live. Valid ids are derived from the nav buttons via `validTabIds()`, so routing never drifts. All parent tabs also route their sub-tabs through the hash (`COMMAND_SUBS` = `command-overview`/`jobs`/`news`/`socials`/`glossary`/`manual-trading`; `MARKET_SUBS` = `market-overview`/`market-signals`/`gapgo`; `ANALYTICS_SUBS` = `performance`/`pnl`/`edge`) via `commandSubTab()`/`marketSubTab()`/`analyticsSubTab()`; `applyTabFromUrl()` recognises every sub-id (`SUBS = MARKET_SUBS.concat(ANALYTICS_SUBS, COMMAND_SUBS)`) and `switchTab()` redirects any of them to the right parent + sub-tab — so all legacy deep links (incl. `#gapgo`, `#pnl`, `#edge`) and keyboard shortcuts keep working. So you can bookmark/share a direct link to any tab or sub-tab, and a browser refresh reopens the last tab instead of defaulting to Command.
- **Live ticker strip** — top-of-page, driven by the **active watchlist** (`getWatchlist()`, up to 20 symbols — no longer a hardcoded 10), price + 24h%, auto-refreshes every 15 s via `/v1beta3/crypto/us/snapshots`. Re-renders immediately on watchlist edits because `saveWatchlistData()` calls `loadTickerStrip()` (roadmap item 1, 2026-06-17).
- **Auto-refresh button** — 3 modes: `Auto OFF` → `Prices 15s` → `Full 60s`.
- **🔒 Read-only badge (2026-07-26, Suite roadmap "live trading read-only")** — `#modeReadOnly` in `header-right`, right of the mode badge, shown by `renderMode()` (`src/js/nav.js`) only while the mode selector is on **Live**. Live Alpaca credentials are insight-only: the trade ticket, Autopilot and every cancel path refuse to run, enforced centrally in `src/js/api-config.js`'s `apiPost`/`apiDelete`. Translated in all 4 languages (`header.readOnly`).
- **Help button / user manual (2026-07-22, Suite roadmap item 1)** — `❓` in the header (`header-right`, next to the theme toggle) opens `#manualPanel`, an off-canvas panel that unfolds from the left (`src/css/manual.css`, `.manual-overlay.open`), matching the pattern CryptoPro Charts already ships. Content is a static `MANUAL_SECTIONS` array in `src/js/manual.js` — one section per tab group plus Account/sign-in and keyboard shortcuts — with a left-rail TOC and a live search filter; no network fetch. Closes via ✕, the panel's own toggle, or Escape.
- **Account button (2026-07-19, Suite SSO; repositioned 2026-08-06)** — `👤 Sign in` in the header, the last/rightmost control in `header-right` — matching CryptoPro Charts/Training/Suite, where the account button is always the trailing element. Opens `#authModalBackdrop` (defined in `client/src/fragments/modals.html`, driven by `src/js/auth.js`) for username/password sign-in/register with optional TOTP 2FA, or the account card (change password / enable-disable 2FA / sign out) once signed in. Shares one accounts database with CryptoPro Charts/Suite/Training — see `CLAUDE.md` › Hosting & frontend.
- **📓 Daily Journal button (header)** — `generateDailyJournal()` builds today's closing journal from live data plus a 10-symbol confluence scan; preview modal with Copy + Download `.md`.

### Tabs

| Tab | Key | Purpose |
|-----|-----|---------|
| 🧭 **Command** | `command` | **Parent tab with Overview + ☁ Scheduled Jobs + 📰 News + 🐦 Socials + 📖 Glossary + 🧪 Manual Trading sub-tabs** (`#command-overview` / `#jobs` / `#news` / `#socials` / `#glossary` / `#manual-trading`). The **☁ Scheduled Jobs sub-tab** (any signed-in account, showing **its own** jobs since multi-tenant Phase 5, 2026-07-28 — owner-only before that; moved out of Overview into its own pane 2026-07-22 per Suite roadmap) shows a banner when the account has no active Alpaca credential (its schedule is inert until then), plus last-run status per Node cron job (evaluate/watchdog; `daily-summary` deleted 2026-07-29) from `/api/cron/status`, a per-job enabled toggle, an hour-of-day (UTC) schedule selector, and a manual "Run now" trigger; `renderCronJobs()` loads lazily on tab entry (same pattern as News/Socials/Glossary) rather than on every Overview refresh. The **Glossary sub-tab** (roadmap 2026-07-18; DB-backed 2026-07-24, scope corrected same day) renders the project's trading-term/acronym reference straight into the dashboard, so definitions are one click away instead of living only in the repo. `loadGlossary(force)` fetches `fetchLocalJson(["/api/glossary"])` — a new route (`src/glossaryRoutes.js`) backed by Postgres (`src/db.js`'s `glossary` table, synced from `memory/glossary.md` on every server boot) — 5-min cache (↻ Refresh forces), and `renderGlossaryMarkdown()` turns the small markdown subset the underlying content uses (headers, tables, `**bold**`, `` `code` ``, `---` rules) into HTML. **Scope (user correction, 2026-07-24):** only `memory/glossary.md`'s "Acronyms & Abbreviations" and "Trading Terms" sections are extracted into the DB row (`src/glossaryExtract.js`) — the file's ~30 other sections are a dated implementation changelog (bug fixes, feature notes), not glossary content, and are excluded. The full file remains the git-tracked edit source unchanged. A search box (`filterGlossary()`) hides non-matching table rows/paragraphs; headers always stay visible. **Bugfix 2026-07-18, superseded 2026-07-24:** `server.js` never statically served `memory/` at all, so `fetch()`-ing the file directly never worked in production regardless of the `file://` issue this bugfix originally targeted — the live dashboard was permanently stuck on the small hardcoded fallback. Moving to `/api/glossary` fixes this: it's reachable in production, and `loadGlossary()` still falls back to a small built-in `GLOSSARY_FALLBACK_MD` constant (curated acronyms + core trading terms) whenever the API call fails, with a yellow status line and a ↻ Refresh prompt to retry — the tab always renders something useful. Read-only, analysis-only — never places orders. The **Socials sub-tab** (v2026-07-09.6; sources fixed 2026-07-10 v2026-07-10.1) shows crypto posts + stats from 14 curated **>0.5M-follower** accounts: post text comes from each account's **official Telegram mirror first** (`SOC_ACCOUNTS[].tg` → `socTgFeedUrl()` → RSS-Bridge TelegramBridge → rss2json; Binance/Watcher.Guru/Whale Alert/Cointelegraph, marked TG) then Nitter-mirror RSS as best-effort (2 mirrors; **feed-title validation** rejects xcancel's fake HTTP-200 "RSS reader not yet whitelisted!" error feed — the 2026-07-10 bug; retweets + media-only TG posts skipped; generalist accounts crypto-keyword-filtered), with the News tab's T1/T2 badges, coin chips, and ⚡ Key-only filter, plus per-account stat chips (@handle · followers · posts with tg/tw suffix, red ✕ when unreachable) and a combined-reach status line; 10-min cache; X links open on x.com, TG links on t.me — analysis-only. The **News sub-tab** aggregates headlines from the Alpaca News API (Benzinga) + CoinDesk/Cointelegraph/Decrypt RSS (via rss2json), deduped by normalized headline + URL, with T1/T2 catalyst badges (per `skills/crypto-catalysts`), base-ticker chips, an All / ⚡ Key-only filter, 5-min cache, and GMT+2 timestamps — analysis-only. The **Overview sub-tab** is the trading-permission cockpit: live hard-rules panel (6 real-time checks), cash-reserve gate, equity/cash/open-risk/drawdown KPIs, trade modal (limit-only). The 🚦 Trading Permission Rules panel carries a **Latest Activity** block in its top-left corner showing the latest 2 FILL activities (`#recentActivities`, rendered by `renderCommand()` from `c.activities`). Under the big trading-status word, `#tradingStatusLog` mirrors the last 3 Autopilot log entries (`apRenderStatusLog()`, kept in sync by `apRenderLog()`). Now also hosts the **🤖 Autopilot** *(layout since v2026-07-10.3, roadmap item 11 v2: the **controls** — OFF-on-load toggle, 15/30/60-min interval, ⛔ kill switch, `#apStatus` — sit at the very top of the sub-tab **above the trading-permission indicator** `#tradingStatus`; the 🤖 Autopilot panel at the bottom keeps only the description + activity log)*: kill switch = stop + cancel all orders + order-age tracker reset, per-cycle entry/exit engine reusing the page's signal scorer with every hard-rule gate, trailing-stop HWM + activity log in `localStorage`. Since 2026-07-08: daily-drawdown gate (entries blocked at −3% from GMT+2 day open), live snapshot quotes for all limit prices, stale-order lifecycle (cancel entries once open ≥ 4h real time — `STRAT_CFG.minStaleEntryAgeHours`, fixed 2026-07-13 from a 1-cycle counter that could fire in ~15 min; cancel-replace exits after 2 cycles), correlation-aware half-sizing (ρ > 0.9), scout promotions in the scan, HWM seeded from `max(localStorage, positions_state.json)`, and all thresholds config-seeded via `STRAT_CFG`. Since 2026-07-09: partial-TP ladder (+1R → 50% out, stop → breakeven; `localStorage.autopilotPartialTp`), stale-position exit (`maxHoldHours`, `localStorage.autopilotEntryTime`), position rotation at a full budget, net-R:R entry gate (**fails closed since 2026-07-29** — an unavailable stop/target now blocks rather than skipping the check), 4H data fallback (`fill4hFallback()`), and the OFF-by-default session-edge filter. Also shows the 🔭 scout-promotions chip (`#scoutChip`), the ⚠ split-HWM warning (`#hwmSplitWarning`), and the red **⚠ BUDGET EXCEEDED chip** (`#budgetChip`, 2026-07-09) when open positions exceed the correlation budget. **Bugfix 2026-07-18 (v2026-07-18.4):** `partial_tp_done`/`entry_time_iso` no longer rely solely on the `data/positions_state.json` merge — that file fetch is blocked under `file://` (same restriction as the Glossary tab), and a silent merge failure let the Autopilot re-fire its own +1R partial-TP sell on an already-reduced position every cycle (a halving-cascade of ever-smaller sells ending in a no-profit breakeven exit). `apReconcileFromFills()` now derives the same facts directly from Alpaca's own FILL ledger via `edgeFetchAllFills()`/`apiFetch()` (an HTTPS call, unaffected by `file://`), run once per cycle before the exit loop. The **🧪 Manual Trading sub-tab** (Suite roadmap item 1, added 2026-08-08) is a paper-trading sandbox — the user opens/closes positions by hand, separate from the automated cron engine and Autopilot. `loadManualTrading()` (`src/js/tabs-manual-trading.js`) scores the union of the tenant's own watchlist (`getWatchlist()`) and any held position on the same 6-point scale the strategy uses, via `ta-lib.js`'s parity-tested `calcSignalScore()` — never the unused, parity-risk `portConfluenceScore()` that already lived, uncalled, in `tabs-portfolio.js`. Rendering reuses that same file's previously-dead `portScoreBar()`/`portActionChip()` helpers (their first real caller), and Buy/Sell/Cover reuse `trade-modal.js`'s `openTradeModal()` unchanged — no new order-placement code. Free plan: Buy is blocked with an upgrade toast once held positions reach `MT_FREE_MAX_OPEN_POSITIONS` (2, mirroring `src/risk.js`'s server-side cap) — client-side courtesy only, same trust level the Portfolio tab's own Buy button has always had. |
| 🔬 **Analytics** | `analytics` | **Parent tab merging Performance + P&L + Edge** under the shared sub-tab bar (`analyticsSubTab()` → generic `_activateSubTab("analytics", …)`). Valid sub-ids: `ANALYTICS_SUBS = ["performance","pnl","edge"]`. Performance auto-loads (`refreshCurrent`→`loadDashboard`); P&L on select (`loadPnl`); Edge manual (▶). Deep links `#performance`/`#pnl`/`#edge` resolve via `applyTabFromUrl()` + `switchTab()` redirect. Lives in the **📊 Analysis** nav section with Backtest + Markov. |
| › 📈 Performance | `performance` (sub) | Equity curve, KPI tiles (Total P&L in dollars, Total Return %, avg return, volatility, best/worst period), rolling metrics, period selector (1M/3M/6M/1Y). |
| ⚠️ **Risk** | `risk` | Portfolio cap usage per symbol (from `config.json`), 10×10 correlation heatmap (Pearson ρ, daily log-returns) shown in the **left** column with Effective Exposure on the right, drawdown/Sharpe/Sortino/Calmar/VaR. |
| ~~Positions~~ | *(removed)* | **Dropped 2026-06-17** — the positions table lives in **Portfolio Overview**. `renderPositions` is kept only to cache `_lastPositions`/`_lastEquity` (Risk concentration panel + CSV export); its DOM writes are null-guarded. |
| 🎯 **Execution** | `execution` | Open/recent orders, cancel-all, limit-band compliance, ATR Position Sizer widget. The Recent Orders table has a sortable **Total** column (USD order value, after Avg Fill — roadmap 2026-07-09, v2026-07-09.4): filled qty × avg fill for (partially) filled orders, else qty × limit price, else the order's notional; "–" when no price is available. **Filter bar added 2026-07-13** (roadmap): Symbol / Type / Side / Status `<select>` filters above the table (`execFilterSymbol`/`execFilterType`/`execFilterSide`/`execFilterStatus`); Symbol/Type/Status populate dynamically from the loaded order set (`populateExecutionFilters()`, selection preserved across refreshes), Side is a static Buy/Sell picker. `applyExecutionFilters()` re-renders the table client-side from the cached `_lastExecutionCtx` (no refetch) with a live "Showing X of Y orders" count; `resetExecutionFilters()` clears all four back to "All". |
| 📡 **Signals** | `signals` | Live 6-point Signal Confluence scanner for the watchlist symbols **+ fresh scout promotions** (SCOUT-tagged rows, 2026-07-08); paginated bar fetch (`barsEnd()` excludes the in-progress bar); trend arrows, ATR qty, **ADX + OBV informational columns** (display-only, not scored), **Spread column** (live bid-ask % from snapshot quotes, 2026-07-09), **Net R:R column** (4H swing-low stop vs BB-upper target **net of round-trip cost** = 2× taker fee + spread, mirrored into the trade modal, 2026-07-09), ⚠ 4H data-quality marker (yellow = synthetic 4H from 1H bars, red = degraded), ⚡ quick-buy, ▶ execute. Min 60 bars before scoring (`STRAT_CFG.minBarsForSignal`). 17 columns. |
| › 💰 P&L | `pnl` (sub) | FIFO realized P&L (shared `computeFifoStats()`), calendar heatmap, attribution by symbol, day-of-week performance, CSV export. |
| 🧪 **Backtest vs Live** | `backtest` | Compares live metrics to saved expected metrics (Sharpe, max DD, win rate, profit factor, avg daily return). Win Rate & Profit Factor use the same realized FIFO stats as the P&L tab. |
| 🌐 **Market** | `market` | **Parent tab merging Market Overview + Scanner + Breakout** under the shared sub-tab bar (`marketSubTab()` → `_activateSubTab("market", …)`). The middle sub-tab is labelled **🔭 Scanner** (renamed from "Signals" so "Signals" names only the watchlist tab; sub-id stays `market-signals`). Valid sub-ids: `MARKET_SUBS = ["market-overview","market-signals","gapgo"]`. The sub-tab id is mirrored to the URL hash + `localStorage.lastTab`, so the legacy deep links `#market-overview` / `#market-signals` / `#gapgo` still open the right sub-tab (resolved in `applyTabFromUrl()`; `switchTab()` also redirects any sub-id to the parent so keyboard shortcuts work). Overview auto-loads (contextual); Scanner + Breakout are manual. Cross-links: "View scanner →" / "← Back to market context". `_marketSub` restores the last sub-tab on re-entry. |
| › 🌍 Market Overview | `market-overview` (sub) | Price, 24h%, 7d%, volume, trend and cap tier per symbol, sortable, with momentum heatmap. Scan universe = the shared `getCryptoUniverse()` filtered to `/USD` pairs (`usdPairsOnly()`, bugfix 2026-07-09 v2) and sliced by the **Max Symbols** setting — no longer hardcoded to 30; symbol cells show the full pair (`BTC/USD`). Score column auto-fills from the last Scanner scan. Each row has a **Trade** column with Buy/Sell buttons (`moTradeButtons()`) that open the shared paper-trade modal pre-filled with symbol, side, and live price. |
| › 🔭 Scanner | `market-signals` (sub) | On-demand full 6-point confluence scan over `getCryptoUniverse()` **filtered to `/USD` pairs** (`usdPairsOnly()`, bugfix 2026-07-09 v2 — USDT/USDC quotes duplicated each base up to 3×; those pairs now live only in the Settings watchlist selector), sliced by the **Max Symbols** setting (no upper clamp). Stablecoin pairs excluded; symbol cells show the full pair (`BTC/USD`). **Per-symbol Watchlist column** (`msWatchlistCell()`): **+ Watch** when score ≥ 4 and not already watched; **– Unwatch** when score ≤ −2 (sell) and no open position; else ✓ watched / –. Buttons update the shared watchlist and re-render in place (`renderMsWatchlistCells()`, no rescan); open positions fetched into `_msOpenPosSyms`. Score distribution + Top Opportunities panel. Scores cached into `_msPrevScores` for cross-tab display. |
| › 📊 Breakout | `gapgo` (sub) | On-demand pre-session breakout/gap analysis per watchlist symbol (folded in from the former standalone tab 2026-06-17): catalyst, supply risk, likelihood, 6-month range position, key levels, historical gap behaviour, trade plan, risk rating. Each card header shows two scores: **Conviction** (gap-specific, max ±7) and **Signal** (standard 6-point `calcSignalScore()` — identical to the Signals tab and the Scanner; the `/6` suffix was dropped 2026-06-29). Manual run (▶ Run Analysis); element `subpage-gapgo`; deep link `#gapgo` preserved. |
| 🔗 **Markov** | `markov` | On-demand first-order Markov chain analysis for BTC/USD & ETH/USD across 30/60/90/180/365-day windows. 3×3 transition matrix, stationary distribution, next-day forecast. Analysis-only — places no orders. |
| 🧠 **Insights** | `insights` | On-demand (▶ Analyze) **behavioral / trading-psychology** read-outs from realized FIFO round-trips (`insRoundTrips()` over paginated FILL history). 4 plain-language cards — 🗓 Day-of-Week Edge, 📉 After Losing Streaks (win rate after 2+ consecutive losses vs baseline), 🔁 Cadence After Outcome (overtrading-after-wins), ⚠ Rule Discipline (best-effort −5% stop + per-symbol cap breaches from trade history) — plus 3 KPI tiles. Analysis-only; in the 📊 Analysis nav section. |
| › Performance KPI note | — | The Performance sub-tab's "Filled Orders" KPI tile was **removed 2026-06-17** (duplicated Execution; misplaced among performance stats). |
| › 🔬 Edge | `edge` (sub) | On-demand (▶ Analyze) realized-edge analytics: FIFO round-trips from all FILL activities — per-symbol expectancy table, P&L by hour-of-day / day-of-week (GMT+2), KPI tiles, factual takeaway line. |
| 📊 **Portfolio Overview** | `port-overview` | Account equity/cash/buying-power/P&L cards; equity curve (Chart.js, period buttons); open positions table (sortable, short-aware). |
| 🥧 **Allocation** | `port-dist` | Donut chart of allocation across positions + cash; sortable breakdown table; cap utilisation table (all watchlist symbols, Over Cap / Near Cap / OK badges). "Over Cap" fires only when rounded utilisation > 100%, matching the displayed "% of cap used"; bar clamped to 100%. |
| ⚙ **Settings** | `settings` | Grouped sections: Paper credentials, Live credentials, Risk Limits, Signals Analysis (**Max Symbols**, default 30, minimum 1, no upper clamp), **🔗 Correlation Budget (Autopilot)** (**Max Open Positions** default 4 + **Max Positions Per Tier** default 3, both min 1; read live each Autopilot cycle via `apMaxPositions()`/`apMaxPerTier()`), and **📋 Active Watchlist** (up to 20 symbols on Pro, `FREE_WATCHLIST_LIMIT` (3) on Free — `addWatchlistSymbol()` blocks past the Free cap with an upgrade toast before the tag is even added; tag editor with Add/Remove/Reset; the add-symbol control is an `<input list>` + `<datalist>` dropdown populated from the full tradable Alpaca exchange universe via `populateWatchlistOptions()` — pick or type to filter; a **Show stablecoins** checkbox (`#watchlistShowStable`, default off) appends `getStablecoinPairs()` to the dropdown so stablecoin pairs (USDT/USD, …) can be opted into the selector — dropdown-only, scan universe unaffected; persisted in `localStorage.proDashboardWatchlist` and synced to Postgres via `/api/session`; used by Autopilot, Daily Journal, Signals tab, Portfolio tabs, **and the cron engine** — `buildTenantContext()` reads this same row as each tenant's own scan universe, replacing the old shared `config.json` watchlist). Seeds from `./config.json`, falling back to the project-root `../config.json` (load-only; also seeds the shared `STRAT_CFG` strategy params via `seedStrategyConfig()` — 2026-07-08); saves to `localStorage`. **Second panel, `.engine-panel` (2026-07-29, multi-tenant Phase 6): ☁ Server-Side Trading Engine** — visually distinct (blue accent rail + tint) because everything in the panel above it is browser-only `localStorage` while this one writes encrypted credentials the scheduled cron engine trades with unattended. Driven by `src/js/settings-engine.js`; loaded by `switchTab("settings")` alongside `loadSettingsForm()`. Per-credential row with an *in use / stored / unreadable here* badge + last-4 preview (`readableHere:false` = encrypted under another environment's key, which the engine skips), Use-this/Disconnect buttons (`data-engine-action` + delegation, never an interpolated inline `onclick`), and a mode/key/secret connect form. Disconnect and replacing the in-use credential show an **inline masked step-up password prompt** (`#engineStepUpEl`, rendered only on a 401 `{stepUp:true}`; not `window.prompt()`, so the password is masked). Collapsible *recent credential activity* section. Guests see a "sign in" line, not an error; any signed-in account (Free or Pro, revised 2026-08-08 — this panel is only `requireSignedIn()`-gated now, capped elsewhere by position/watchlist limits, not blocked) can connect credentials and run scheduled jobs. **The strategy-override JSON editor that used to live in this panel was removed (2026-08-06, Suite roadmap) — too confusing for beginner traders without IT knowledge.** `PUT/GET/DELETE /api/strategy-config` (`src/strategyConfigRoutes.js`, `src/userConfig.js`'s `CONFIG_SPEC`) stays live server-side and is still the per-tenant config `src/tenantEngine.js` resolves from — it simply has no UI surface anymore. |

### Shared crypto universe (Market Overview + Market Signals)

- `getCryptoUniverse()` fetches the full tradable-crypto list once (`/v2/assets?asset_class=crypto&status=active`), caches it in `_cryptoUniverse`, and orders it as the still-tradable `TOP30_SYMBOLS` first then every other USD pair alphabetically. Falls back to `TOP30_SYMBOLS` only if the call fails or yields nothing.
- **Robust to symbol format** — accepts both `BTC/USD` and bare `BTCUSD`, normalizes to `BASE/QUOTE`. The universe keeps USD/USDT/USDC quotes (`ALLOWED_QUOTES`, 2026-06-19), but the **scan surfaces (Scanner + Market Overview) filter to `/USD` pairs via `usdPairsOnly()`** (bugfix 2026-07-09 v2 — Alpaca trades against USD; the mixed quotes duplicated each base up to 3× in results). USDT/USDC pairs remain only in the Settings watchlist selector.
- **Max Symbols setting** (`maxSignalSymbols`) drives how many symbols *both* pages scan (`usdPairsOnly(universe).slice(0, n)`). It has **no upper limit** — the only ceiling is how many `/USD` pairs the account can trade (`updateScanBtnLabel()` clamps against the USD-only count).
- **Contiguous ranks** — `rebuildUniverseRank()` builds `_universeRank` (sym → 1-based universe position); the shared `symbolInfo(sym)` helper returns curated `TOP30_INFO` when known, else a fallback whose `rank` is the universe position. So ranks are 1–30 (cap ranks) then 31+ (universe order) instead of `#?`. Symbols beyond the top 30 still show cap tier `?`.

### Keyboard shortcuts

`1`–`9` select the first nine tabs (Command…Settings order); `R` refreshes the current tab.

## Original Design Reference

The sections below are the original professional-dashboard design notes that shaped the tab structure above.
Kept as reference for future feature additions.

### Executive cockpit — "Am I allowed to trade today?"

| Metric | Why it matters |
|--------|---------------|
| Account equity / NAV | Current capital base |
| Today P&L | Whether the day is within normal range |
| Open risk | Capital at risk if all stops are hit |
| Daily loss limit status | OK / Warning / Stop trading |
| Current drawdown | How far below recent equity peak |
| Max drawdown | Largest peak-to-trough loss ever |
| Trading allowed? | Trade / Reduce size / Stop |

### Performance metrics

| Metric | Explanation |
|--------|-------------|
| Net P&L | Result after costs |
| Cumulative P&L / equity curve | Account development over time |
| Win rate | % of profitable trades (read alongside payoff ratio) |
| Average win / average loss | Are winners larger than losers? |
| Profit factor | Gross profit ÷ gross loss |
| Expectancy / Average R | Expected earn/loss per trade |

### Risk metrics

| Metric | Why it matters |
|--------|---------------|
| Max drawdown | Largest peak-to-trough loss |
| Current drawdown | Currently underwater? |
| Sharpe ratio | Risk-adjusted return |
| Sortino ratio | Downside-risk-adjusted return |
| Calmar ratio | Return vs max drawdown |
| VaR / CVaR | Loss risk at chosen confidence level |

### Execution quality

| Metric | Why |
|--------|-----|
| Slippage | Execution drag |
| Commissions / fees | Cost drag on edge |
| Planned vs actual entry | Execution discipline |
| R-multiple at entry | Standardised risk/return |

### Alerts the dashboard enforces

| Alert | Meaning |
|-------|---------|
| Daily loss limit hit | Stop trading for the day |
| Drawdown exceeds threshold | Reduce size or pause |
| Risk per trade too high | Position too large |
| Exposure concentration high | Too much in one instrument |
| Strategy underperforming vs expected | Edge may be changing |
